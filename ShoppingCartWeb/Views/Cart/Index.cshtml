@model List<ShoppingCartWeb.Models.CartItem>

<h2>Your Cart</h2>

@if (Model.Count == 0)
{
    <p>Your cart is empty.</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Product</th>
                <th>Quantity</th>
                <th>Price</th>
                <th>Total</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Product.Name</td>
                    <td>@item.Quantity</td>
                    <td>@item.Product.Price.ToString("C")</td> <!-- Format Price as Currency -->
                    <td>@((item.Product.Price * item.Quantity).ToString("C"))</td> <!-- Correct way to format the total -->

                    <td>
                        <!-- Remove from Cart button -->
                        <form method="post" asp-action="RemoveFromCart" asp-route-id="@item.CartItemId">
                            <button type="submit" class="btn btn-danger">Remove</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <h3>Total Cost: @ViewBag.CartTotal.ToString("C")</h3> <!-- Format Total Cost as Currency -->

    <a href="/Products" class="btn btn-primary">Continue Shopping</a>
}
